import React, { FC } from "react";
export interface RateProps { }

const Rate: FC<RateProps> = (props) => {
  return (
    <>
      <div>Rate</div>
    </>
  );
};

Rate.defaultProps = {};

export default Rate;
