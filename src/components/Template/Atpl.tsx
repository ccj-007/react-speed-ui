import React, { FC } from "react";
export interface ProgressProps {}

const Atpl: FC<ProgressProps> = (props) => {
	return (
		<>
			<div>Atpl</div>
		</>
	);
};

Atpl.defaultProps = {};

export default Atpl;
